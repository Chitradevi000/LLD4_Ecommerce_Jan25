spring.application.name=EcommerceProductService
management.endpoints.web.exposure.include=mappings

#This is the copy of main resources
#I can make a relavant changes for H2

#Datasource Properties
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.show-sql=true
spring.jpa.show-sql=true
spring.datasource.url=${PRODUCTSERVICE_JDBC_URL}
spring.datasource.username=${PRODUCTSERVICE_JDBC_USERNAME}
spring.datasource.password=${PRODUCTSE$RVICE_JDBC_PASSWORD}
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
